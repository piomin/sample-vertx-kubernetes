apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: account-service
spec:
  hosts:
    - account-service
  http:
  - fault:
      delay:
        fixedDelay: 1s
        percent: 100
    route:
    - destination:
        host: account-service
        subset: app-account-service